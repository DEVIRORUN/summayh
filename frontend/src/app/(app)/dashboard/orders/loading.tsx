export default function Loading() {
    return (
        <div className="flex flex-col gap-4 p-6">
            {/* a few skeleton rows, however you style loading placeholders elsewhere */}
        </div>
    );
}